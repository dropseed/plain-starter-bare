[project]
name = "app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "~=3.12.0"
dependencies = [
    "plain",
]

[tool.uv]
dev-dependencies = [
    "plain-dev",
    "plain-pytest",
]
